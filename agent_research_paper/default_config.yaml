dataset:
  vox1_dir: data/vox1
  user_data_dir: data/user
model:
  base_model: openai/whisper-small
training:
  lora_rank: 16
  lr: 1e-4
  epochs: 4
  recovery_cycles: 2
pruning:
  epsilon: 0.01
evaluation:
  batch_size: 8
  attack_types: ['replay','deepfake','adversarial']
  threshold: 0.8
